{{ form_start(form) }}
    {{ form_widget(form) }}
    <hr>
    <button id="save-post-btn" name="save-post-btn" type="submit" class="btn btn-info">{{ button_label|default('Save') }}</button>
    <button id="clear-post-btn" name="clear-post-btn" type="reset" class="btn btn-default ">clear</button>
{{ form_end(form) }}
